# Codebase Research

## Findings

- The application directory had no source files, package manifest, build configuration, tests, README, or design system before this run.
- `MEMORY.md` and the Codex conversation scaffold are workspace metadata, not application code.
- No existing sorter, scoring engine, API, or UI component was found to improve or extend.

## Classification

`new product` — implementation can establish the first project structure without reimplementing an existing module.

## Constraints inherited from the workspace

- Work inside `/var/home/tcs/Code/ideologynormativedescriptiveprescriptivesorter`.
- Preserve all Zeus planning, verification, QA, and report artifacts under the project directory.
- Keep the product client-only unless a later approved scope change adds a server boundary.
