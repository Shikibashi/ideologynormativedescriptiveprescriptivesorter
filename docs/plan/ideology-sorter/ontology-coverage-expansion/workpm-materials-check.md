# WorkPM materials check — ontology coverage expansion

## Result

PASS_WITH_HOLDS. The WorkPM sequential fallback completed the research, proposal, implementation, governance, and verification contract. Native explorer/worker delegation was unavailable in the desktop tool surface, so the Lead executed the bounded work sequentially. No MCP task board or hard file lock was needed.

## Evidence

- `npm run research:coverage` reports 119 targets, 1,428 candidates across all 119 targets, and zero candidate/governance validation errors.
- `src/research-governance.ts` records one explicit taxonomy decision for every ontology or registry target.
- `npm run test:run` passes 35/35; `npm run build` passes with the existing Vite large-client-chunk advisory.
- The structural measurement fixture reaches all 16 existing production anchors through descriptive, normative, and prescriptive layers without changing production data.
- `npm run qa` passes 10/10 locally and Docker-backed QA passes 10/10.
- Compose config, Docker build, health endpoint, and container health pass.

## Holds

- Candidate coverage is closed for all 119 targets. The measurement ledger still contains 91 catalog-only targets, 15 scored-indirect targets, 12 registry-only targets, and one dedicated-scored target; only Right-Libertarianism has explicit target-tagged production coverage.
- The completion tranche remains quarantined research material and does not turn any catalog or registry entry into a production measure.
- No cognitive review, respondent interview, simulated substitute, empirical reliability, validity, or invariance evidence was run.
- The preceding combined-pattern cycle left the production bank at 84 questions; the active continuation now has 156 questions, 52 per layer.

## Current continuation — combined pattern — 2026-08-26

### WorkPM result

PASS_WITH_HOLDS. The current cycle used the WorkPM sequential fallback after source review: it added an academic morphology source record, extended the typed result contract with a gated combined pattern, preserved all three layer readings, and bumped the scoring-policy version to 3. No cognitive review or respondent substitute was used.

### Evidence

- Freeden's morphology source and Treier/Hillygus multidimensionality source are recorded as construct context; neither validates local anchor vectors or classifies a respondent.
- The combined result requires all three covered layers, gives each layer equal weight, exposes per-layer contributions, and is tested against a one-anchor fixture for exact equal averaging.
- Missing-layer unit and browser fixtures preserve the covered layer results while withholding the combined pattern.
- `npx tsc --noEmit --pretty false` passes; `npm run test:run -- --reporter=dot` passes 37/37; `npm run qa` passes 10/10.
- The Docker image was rebuilt from the current source; Compose config, health endpoint, container health, and Docker-backed `E2E_BASE_URL=http://127.0.0.1:8001 npm run qa` pass, with 10/10 browser scenarios.

### Holds

- Candidate coverage remains research-only and target-specific measurement remains incomplete despite the 119/119 metadata ledger.
- No cognitive review, respondent evidence, substitute simulation, empirical reliability/validity, psychometric calibration, production promotion, commit, or push was performed.

## Current continuation — direct branch coverage — 2026-08-26

### WorkPM result

PASS_WITH_HOLDS. Source review and the existing research ledger supported a bounded direct-coverage tranche for six already-defined ontology targets. The implementation added 72 original prompts, kept them layer-contiguous, and preserved the distinction between target-tagged production prompts and quarantined research candidates. No new ideology node was added because the measured gap was direct coverage of existing nodes, not a missing taxonomy concept.

### Evidence

- Content version 4 contains 156 questions, 52 per layer, with seven dedicated-scored targets: Right-Libertarianism plus Classical Liberalism, Social Liberalism, Moderate Conservatism, Social Democracy, Democratic Socialism, and Minarchism.
- `npm run research:coverage` reports 119 targets, 1,428 candidates, seven dedicated-scored targets, nine scored-indirect targets, 91 catalog-only targets, 12 registry-only targets, and zero research/governance validation errors.
- The new branch blocks each contain four descriptive, four normative, and four prescriptive target-tagged prompts; all candidate records remain `research_candidate` and all registry entries remain outside production targeting.
- `npm run test:run -- --reporter=dot` passes 39/39, `npm run build` passes, and `npm run qa` passes 10/10 after the layer-contiguous ordering fix.

### Holds

- The six blocks are editorial source-backed additions, not cognitive, respondent, psychometric, reliability, validity, or population evidence.
- Academic sources support terminology, construct boundaries, and authoring rationale; they do not validate local wording or anchor vectors.
- Further canonicalization requires substantive neighbor-distinctness review, applicable cross-cultural/jurisdictional review or a not-applicable rationale, and later empirical validation.

## Current continuation — four canonical branch blocks — 2026-08-26

### WorkPM result

PASS_WITH_HOLDS. The current source-backed continuation adds 48 original production prompts across four already-defined canonical targets: Ecosocialism, Left-Libertarianism, Libertarian Socialism, and National Conservatism. Each target receives four descriptive, four normative, and four prescriptive prompts, with direct target tags and existing anchor paths. No ontology node or registry entry was added because the audit identified a direct-coverage gap in existing nodes rather than a demonstrated missing concept.

### Evidence

- The active manifest is content version 5 with 204 production prompts, 68 per claim layer, 16 editorial anchors, and 11 `dedicated-scored` target blocks.
- The target ledger reports 91 `catalog-only`, 5 `scored-indirect`, 12 `registry-only`, and 11 `dedicated-scored` targets. All 1,428 candidates across 119 targets remain quarantined as `research_candidate` records.
- `npm run research:coverage --silent` reports 119 profile rows, 119 false-positive audits, and zero research/governance validation errors. Each new branch has 12 direct questions with 4/4/4 layer counts.
- Provenance was expanded with Löwy's Cambridge ecosocialism chapter; the other branch boundaries use the existing SEP, Oxford, Ostrom, Sen, and International Affairs records. These sources support construct boundaries and authoring rationale only.
- `npm run test:run -- --reporter=dot` passes 40/40, including a complete 204-answer share round trip. `npx tsc --noEmit --pretty false` and `npm run build` pass; the existing Vite large-client-chunk advisory remains.
- Local Playwright passes 10/10. The rebuilt Docker image is healthy, `/healthz` returns `ok`, and Docker-backed Playwright passes 10/10.
- `npm audit --audit-level=high` reports zero vulnerabilities. The share decoder bound is now `MAX_FRAGMENT_LENGTH = 16384`, which covers the current complete answer hash measured at 12,850 characters without removing the finite input guard.

### Holds

- The four blocks are source-backed editorial coverage, not cognitive, respondent, psychometric, reliability, validity, invariance, or population evidence.
- `dedicated-scored` is a local coverage/traceability state and does not mean the branch is empirically validated or that a respondent belongs to the label.
- Further promotion still requires substantive neighbor-distinctness review, applicable cross-cultural/jurisdictional review or a documented not-applicable rationale, and later empirical validation. No cognitive review or substitute evidence was run.
- The repository remains on `master` with zero commits; no commit or push was performed.
