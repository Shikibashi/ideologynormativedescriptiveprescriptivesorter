# Argos boundary report — ontology coverage expansion

## Result

PASS_WITH_HOLDS. Static, provenance, quarantine, and governance checks pass. The remaining holds are scope and evidence limits, not hidden activation.

## Verified

- The ontology contains 9 canonical macro, 33 canonical meso, and 60 canonical micro nodes, plus 12 registry entries.
- The derived target inventory covers all 119 targets.
- The candidate bank contains 1,428 unique effect-free records across all 119 targets, with four items in each claim layer per target.
- All 119 targets have a qualitative profile, at least two neighbor discriminants, a false-positive audit, and a coverage summary.
- The governance ledger contains one source-backed decision per target and validates with zero errors.
- Khomeinism and Qutbism are explicitly promoted to canonical catalog status without anchors; Deep Ecology and Bioregionalism are explicitly demoted to associated registry status without scoring.
- Production questions, effects, anchors, thresholds, manifests, share envelopes, and result labels remain unchanged.
- The structural reachability fixture routes all 16 existing production anchors through all three layers and confirms the 28-question-per-layer contract; it is technical route evidence only, not respondent or cognitive evidence.

## Holds

- The current evidence supports taxonomy and authoring decisions, not psychometric validity or respondent classification.
- Candidate coverage is closed at 119/119, but production measurement remains incomplete: 91 targets are catalog-only, 15 are scored-indirect, 12 are registry-only, and only one target has a dedicated scored block.
- The completion tranche is source-backed research material, not empirical validation; no cognitive review, respondent evidence, or substitute evidence was run.
- Accessibility and security follow-up items from the prior Argos cycle remain open.

## Current continuation — combined pattern — 2026-08-26

The result contract now includes a separate `combined` state. It is `insufficient-information` until all three layers meet the existing threshold; when covered, it averages one full-precision proximity value from each layer equally and exposes those three contributions on every combined neighbor. The separate layer results, taxonomy paths, source links, and cross-layer pulls remain intact.

The policy version is 3 so old share fragments cannot be silently interpreted under the changed derived-result semantics. No question wording, question effects, anchor vectors, research candidate status, ontology status, or promotion gate was changed. This is a transparent product calculation, not an empirical classifier, identity verdict, recommendation, or comparison/ranking of political ideologies.

### Current verification

- `npx tsc --noEmit --pretty false`: passed.
- `npm run test:run -- --reporter=dot`: passed, 37/37.
- `npm run qa`: passed, 10/10, including full covered and missing-layer combined-pattern assertions.

### Current holds

- The 1,428 candidate records remain quarantined research material; all 119 target metadata rows are closed, but target-level production measurement remains 91 catalog-only, 15 scored-indirect, 12 registry-only, and one dedicated-scored target.
- No cognitive review, respondent evidence, substitute simulation, empirical reliability/validity, or psychometric calibration was run or claimed.

## Current continuation — direct branch coverage — 2026-08-26

### Result

PASS_WITH_HOLDS. The direct-coverage continuation passes the static, provenance, quarantine, scoring-contract, and browser-route checks. Its remaining holds are the same authority and evidence boundaries: the new prompts are editorial source-backed material, not validated respondent measures.

### Verified

- Content version 4 contains 156 original questions, 52 per layer, and 16 editorial anchors.
- Seven existing targets are `dedicated-scored`: Right-Libertarianism plus Classical Liberalism, Social Liberalism, Moderate Conservatism, Social Democracy, Democratic Socialism, and Minarchism. The remaining target states are 9 `scored-indirect`, 91 `catalog-only`, and 12 `registry-only`.
- Each of the six new target blocks contains four descriptive, four normative, and four prescriptive questions with academic ideology-research source references and explicit target tags.
- The question sequence is layer-contiguous, preserving manifest-derived transition boundaries and the separate descriptive/normative/prescriptive contract.
- `npm run research:coverage` reports 1,428 quarantined candidates across all 119 targets and zero candidate/governance validation errors.
- `npm run test:run -- --reporter=dot` passes 39/39; `npx tsc --noEmit --pretty false` and `npm run build` pass with the existing large-client-chunk advisory.
- Local Playwright passes 10/10; the recreated Docker container is healthy, `/healthz` returns `ok`, and Docker-backed Playwright passes 10/10.
- `npm audit --audit-level=high` reports zero vulnerabilities; Compose configuration and JSON/whitespace integrity checks pass.

### Holds

- No cognitive review, respondent interview, substitute simulation, empirical reliability/validity, invariance, or psychometric calibration was run or claimed.
- Academic sources support terminology, construct boundaries, and item-authoring rationale only; they do not validate local wording or anchor vectors.
- Further promotion requires substantive neighbor-distinctness review, applicable cross-cultural/jurisdictional review or a not-applicable rationale, and later empirical validation.

## Current continuation — four canonical branch blocks — 2026-08-26

### Result

PASS_WITH_HOLDS. Static, provenance, quarantine, scoring-contract, share, and browser-route checks pass for the current v5 continuation. The remaining holds are authority and evidence boundaries, not hidden activation.

### Verified

- Content version 5 contains 204 original production questions, 68 per layer, and 16 editorial anchors.
- Eleven existing targets are `dedicated-scored`: Right-Libertarianism, the six earlier direct branches, Ecosocialism, Left-Libertarianism, Libertarian Socialism, and National Conservatism. The remaining target states are 5 `scored-indirect`, 91 `catalog-only`, and 12 `registry-only`.
- Each of the four new target blocks contains four descriptive, four normative, and four prescriptive source-linked questions with explicit target tags and existing anchor paths.
- The current research audit reports 1,428 quarantined candidates across 119 targets, 119 profiles, 119 false-positive audits, and zero candidate/governance validation errors.
- `npm run test:run -- --reporter=dot` passes 40/40; `npx tsc --noEmit --pretty false` passes; `npm run build` passes with the existing large-client-chunk advisory.
- Local Playwright passes 10/10. The recreated Docker container is healthy, `/healthz` returns `ok`, and Docker-backed Playwright passes 10/10.
- The share contract now accepts a bounded 16,384-character fragment, enough for the measured 12,850-character complete v5 answer hash; malformed and oversized inputs remain fail-closed.
- `npm audit --audit-level=high` reports zero vulnerabilities.

### Holds

- Academic sources support terminology, construct boundaries, and item-authoring rationale only; they do not validate local wording, anchor vectors, respondent classification, or a political comparison/ranking.
- No cognitive review, respondent evidence, substitute simulation, empirical reliability/validity, invariance, or psychometric calibration was run or claimed.
- Further promotion requires substantive neighbor-distinctness review, applicable cross-cultural/jurisdictional review or a not-applicable rationale, and later empirical validation.
